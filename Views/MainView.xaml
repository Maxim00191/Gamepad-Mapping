<UserControl x:Class="Gamepad_Mapping.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sections="clr-namespace:Gamepad_Mapping.Views.Sections"
             mc:Ignorable="d"
             Focusable="True"
             Background="{DynamicResource AppBackgroundBrush}"
             Foreground="{DynamicResource AppTextBrush}">
    <Grid Margin="12"
          Background="{DynamicResource AppBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="360" />
            <ColumnDefinition Width="*" MinWidth="280" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Column="0" Grid.RowSpan="2">
            <StackPanel DockPanel.Dock="Top">
                <sections:ProfileTemplatePanelView DataContext="{Binding ProfileTemplatePanel}" />
                <sections:NewBindingPanelView DataContext="{Binding NewBindingPanel}" />
            </StackPanel>

            <sections:MappingEditorView DataContext="{Binding MappingEditorPanel}" />
        </DockPanel>

        <sections:GamepadMonitorView Grid.Column="1"
                                     Grid.Row="0"
                                     Grid.RowSpan="2"
                                     Margin="12,0,0,0"
                                     DataContext="{Binding GamepadMonitorPanel}" />
    </Grid>
</UserControl>

