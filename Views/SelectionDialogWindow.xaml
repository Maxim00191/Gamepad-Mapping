<Window x:Class="Gamepad_Mapping.Views.SelectionDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="640"
        Height="560"
        MinWidth="560"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppTextBrush}">
    <Border Padding="16"
            Background="{DynamicResource AppSurfaceBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="0,0,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Margin="0,0,0,4"
                           Text="{Binding Source={StaticResource Loc}, Path=[KeyboardActionPicker_SearchLabel]}" />

                <Grid Grid.Row="1">
                    <TextBox x:Name="SearchTextBox"
                             ToolTip="{Binding SearchPlaceholder}"
                             Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Margin="10,6,26,6"
                               IsHitTestVisible="False"
                               Foreground="{DynamicResource AppSecondaryTextBrush}"
                               Text="{Binding SearchPlaceholder}"
                               TextTrimming="CharacterEllipsis">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>

            <ListBox Grid.Row="1"
                     Style="{StaticResource AppSelectionDialogListBoxStyle}"
                     ItemContainerStyle="{StaticResource AppSelectionDialogListBoxItemStyle}"
                     ItemsSource="{Binding VisibleItems}"
                     SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                     MouseDoubleClick="ListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding PrimaryText}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="1"
                                       Margin="8,0,0,0"
                                       Foreground="{DynamicResource AppSecondaryTextBrush}"
                                       Text="{Binding SecondaryText}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2"
                        Margin="0,12,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="{Binding Source={StaticResource Loc}, Path=[Cancel]}"
                        IsCancel="True"
                        Margin="0,0,8,0" />
                <Button Content="{Binding Source={StaticResource Loc}, Path=[Dialog_Select]}"
                        IsDefault="True"
                        Click="Select_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
